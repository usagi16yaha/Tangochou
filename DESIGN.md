# 単語帳アプリ 設計ドキュメント

## 概要

PC・スマホ両対応の単語帳アプリ。
単語データはNode.jsサーバーのSQLiteデータベースで管理する。

---

## ファイル構成

```
dic/
├── server.js          ← Node.js + Express サーバー
├── package.json       ← npmの設定ファイル
├── DESIGN.md          ← このファイル（設計メモ）
├── data/
│   └── words.db       ← SQLiteデータベース（自動生成）
└── public/
    ├── index.html     ← 画面の構造（HTML）
    ├── style.css      ← デザイン（CSS）
    └── script.js      ← 動作ロジック（JavaScript）
```

---

## 起動方法

```
npm install
node server.js
```

ブラウザで `http://localhost:3000` を開く。

---

## サーバー設計（server.js）

### 使用パッケージ

| パッケージ | 役割 |
|---|---|
| express | HTTPサーバー |
| better-sqlite3 | SQLite操作（同期式） |

### REST API エンドポイント

| メソッド | URL | 処理 |
|---|---|---|
| GET | /api/words | 全単語を取得 |
| POST | /api/words | 単語を追加 |
| PUT | /api/words/:id | 単語を更新 |
| DELETE | /api/words/:id | 単語を削除 |

---

## データベース設計（SQLite）

**ファイル:** `data/words.db`

**テーブル: cards**

| カラム | 型 | 説明 |
|---|---|---|
| id | INTEGER | 主キー（自動採番） |
| question | TEXT | 問題（表側） |
| answer | TEXT | 答え（裏側） |

初回起動時にサンプルデータ5問を自動挿入。

---

## 画面設計

### 1. 勉強画面（デフォルト）

```
┌──────────────────────────┐
│         単語帳           │
│  [勉強する] [単語を管理] │
├──────────────────────────┤
│                          │
│  ┌────────────────────┐  │
│  │       apple        │  │
│  │                    │  │
│  │ ↑ スワイプで答えを │  │
│  └────────────────────┘  │
│         1 / 5            │
└──────────────────────────┘
```

答えを表示した後:

```
│  ┌────────────────────┐  │
│  │       りんご       │  │
│  │                    │  │
│  │ [わかった✓] [もう一度✗] │
│  └────────────────────┘  │
```

### 2. 単語管理画面

```
┌──────────────────────────┐
│  [＋ 単語を追加]         │
│                          │
│  apple → りんご  [編集][削除] │
│  cat   → ねこ    [編集][削除] │
│  ...                     │
└──────────────────────────┘
```

---

## 操作方法

### PC
| 操作 | 効果 |
|---|---|
| スペースキー（問題画面） | 答えを表示 |
| スペースキー（答え画面） | 次の問題へ |

### スマホ
| 操作 | 効果 |
|---|---|
| 上スワイプ（問題画面） | 答えを表示 |
| 上スワイプ（答え画面） | 次の問題へ |

---

## スコア機能

- 「わかった」「もう一度」ボタンでカウント
- 全カードを1周したら正解数・正解率を表示
- スコアはセッション中のみ保持（データベースには保存しない）
